# 🚀 Как запустить Stoloto AI Lottery Assistant

## Быстрый запуск (каждый раз)

### 1. Откройте терминал в папке проекта
```bash
cd /Users/dom/Desktop/hackMSK52
```

### 2. Запустите PostgreSQL (если не запущен)
```bash
brew services start postgresql@14
```

### 3. Запустите Ollama (если не запущен)
Откройте **новый терминал** и выполните:
```bash
ollama serve
```
Оставьте этот терминал открытым.

### 4. Активируйте виртуальное окружение и запустите приложение
В основном терминале:
```bash
source venv/bin/activate
python app.py
```

### 5. Откройте в браузере
```
http://localhost:5001
```

---

## 🛑 Как остановить проект

1. В терминале с приложением нажмите **Ctrl+C**
2. Деактивируйте виртуальное окружение:
```bash
deactivate
```

---

## 📋 Первый запуск (только один раз)

Если вы запускаете проект первый раз после перезагрузки компьютера или клонирования:

### 1. Установите зависимости
```bash
cd /Users/dom/Desktop/hackMSK52
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### 2. Установите PostgreSQL
```bash
brew install postgresql@14
brew services start postgresql@14
```

### 3. Создайте базу данных
```bash
createdb stoloto_db
```

### 4. Инициализируйте схему базы данных
```bash
source venv/bin/activate
python app.py --init-db
```

### 5. Установите Ollama и модель
```bash
# Скачайте Ollama с https://ollama.ai
ollama pull llama3.2
```

---

## ⚙️ Полезные команды

### Проверить статус PostgreSQL
```bash
brew services list | grep postgresql
```

### Перезапустить PostgreSQL
```bash
brew services restart postgresql@14
```

### Проверить, запущена ли Ollama
```bash
curl http://localhost:11434/api/tags
```

### Посмотреть доступные модели Ollama
```bash
ollama list
```

### Очистить базу данных и создать заново
```bash
dropdb stoloto_db
createdb stoloto_db
python app.py --init-db
```

---

## 🔍 Решение проблем

### Ошибка: "Port 5001 is already in use"
```bash
# Найти процесс на порту 5001
lsof -ti:5001

# Остановить процесс
kill -9 $(lsof -ti:5001)
```

### PostgreSQL не запускается
```bash
# Остановить все процессы PostgreSQL
brew services stop postgresql@14

# Очистить lock файлы
rm /usr/local/var/postgresql@14/postmaster.pid

# Запустить снова
brew services start postgresql@14
```

### Ollama не отвечает
```bash
# Закройте терминал с Ollama (Ctrl+C)
# Запустите заново
ollama serve
```

---

## 📱 Доступные страницы

После запуска приложения:

- **Главная**: http://localhost:5001/
- **Регистрация**: http://localhost:5001/register
- **Чат** (основной): http://localhost:5001/chat
- **Тест чата** (без авторизации): http://localhost:5001/test-chat
- **Dashboard**: http://localhost:5001/dashboard
- **Все лотереи**: http://localhost:5001/lotteries

---

## 💡 Советы

1. **Всегда активируйте venv** перед запуском:
   ```bash
   source venv/bin/activate
   ```

2. **Ollama должна работать** в отдельном терминале для чат-бота

3. **PostgreSQL должен быть запущен** для работы с базой данных

4. **Порт 5001** - основной порт приложения (можно изменить в `.env`)

---

## 🎯 Минимальный запуск (3 команды)

```bash
# 1. PostgreSQL
brew services start postgresql@14

# 2. Ollama (в новом терминале)
ollama serve

# 3. Приложение (в основном терминале)
cd /Users/dom/Desktop/hackMSK52 && source venv/bin/activate && python app.py
```

Готово! Приложение на http://localhost:5001 ✨
