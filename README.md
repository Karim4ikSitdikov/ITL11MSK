# Веб-сервис помощник по выбору лотерей Столото

Полноценный веб-сервис с Flask backend, PostgreSQL базой данных и интеграцией с локальной LLM (Ollama) для персонализированных рекомендаций лотерей на основе данных с сайта Stoloto.

## 🎯 Особенности

- **Персонализированные рекомендации**: AI-ассистент на базе Ollama подбирает лотереи согласно вашим предпочтениям
- **Статистика и аналитика**: Подробная информация о вероятностях выигрыша, призовых фондах и истории тиражей
- **Чат-бот с контекстом**: Умный помощник, который учитывает вашу историю и предпочтения
- **Минималистичный дизайн**: Черно-желтая тема со свечением курсора и плавными анимациями
- **Парсинг данных Stoloto**: Автоматическое обновление информации о лотереях

## 📋 Требования

- Python 3.9+
- PostgreSQL 13+
- Ollama (уже установлена локально)
- macOS/Linux

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
# Создайте виртуальное окружение
python3 -m venv venv
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

### 2. Настройка PostgreSQL

```bash
# Создайте базу данных
createdb stoloto_db

# Или используйте psql
psql -c "CREATE DATABASE stoloto_db;"
```

### 3. Настройка переменных окружения

```bash
# Скопируйте пример конфигурации
cp .env.example .env

# Отредактируйте .env и укажите ваши настройки:
# - DATABASE_URL: URL подключения к PostgreSQL
# - SECRET_KEY и JWT_SECRET_KEY: секретные ключи (генерируйте случайные)
# - OLLAMA_HOST и OLLAMA_MODEL: настройки Ollama
```

### 4. Инициализация базы данных

```bash
# Создайте схему БД
python app.py --init-db
```

### 5. Загрузка данных о лотереях

```bash
# Загрузите данные (будут использованы fallback данные для демо)
python parsers/db_loader.py --all

# Или только лотереи
python parsers/db_loader.py --lotteries
```

### 6. Проверка Ollama

```bash
# Убедитесь, что Ollama запущена и доступна модель llama3.2
ollama list

# Если модель не загружена, загрузите её
ollama pull llama3.2
```

### 7. Запуск приложения

```bash
python app.py
```

Приложение будет доступно по адресу: http://localhost:5000

## 📚 Использование

### Первый запуск

1. Откройте http://localhost:5000
2. Нажмите "Зарегистрироваться"
3. Заполните email и пароль
4. Укажите свои предпочтения (бюджет, тип выигрыша, размер приза)
5. Система автоматически подберет для вас подходящие лотереи

### Основные функции

- **Dashboard**: Ваша главная страница с рекомендованными лотереями
- **Все лотереи**: Полный список с фильтрацией и сортировкой
- **Чат**: Общайтесь с AI-помощником для поиска лотерей
- **Настройки**: Изменяйте свои предпочтения в любое время

## 🏗️ Архитектура проекта

```
hackMSK52/
├── app.py                 # Главный файл Flask приложения
├── config.py             # Конфигурация
├── requirements.txt      # Зависимости Python
│
├── database/             # Модуль работы с БД
│   ├── db.py            # Менеджер подключений
│   └── schema.sql       # SQL схема
│
├── routes/              # API маршруты
│   ├── auth.py          # Аутентификация
│   ├── preferences.py   # Предпочтения
│   ├── chat.py          # Чат-бот
│   ├── lotteries.py     # Лотереи
│   └── analytics.py     # Аналитика
│
├── llm/                 # Интеграция с Ollama
│   └── ollama_client.py # Клиент LLM
│
├── recommendations/     # Рекомендательная система
│   └── engine.py        # Алгоритм подбора
│
├── parsers/             # Парсинг данных
│   ├── stoloto_parser.py # Парсер Stoloto
│   └── db_loader.py     # Загрузчик в БД
│
├── static/              # Статические файлы
│   ├── css/
│   │   └── style.css    # Стили (черно-желтая тема)
│   └── js/
│       ├── api.js       # API клиент
│       └── cursor-glow.js # Эффект свечения курсора
│
└── templates/           # HTML шаблоны
    ├── landing.html     # Главная страница
    ├── register.html    # Регистрация
    ├── dashboard.html   # Личный кабинет
    ├── lotteries.html   # Список лотерей
    └── chat.html        # Чат-бот
```

## 🔧 API Endpoints

### Аутентификация
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `GET /api/auth/me` - Текущий пользователь

### Предпочтения
- `GET /api/preferences` - Получить предпочтения
- `PUT /api/preferences` - Обновить предпочтения

### Чат
- `POST /api/chat` - Отправить сообщение
- `GET /api/chat/history` - История диалогов

### Лотереи
- `GET /api/lotteries` - Все лотереи
- `GET /api/lotteries/<id>` - Детали лотереи
- `GET /api/lotteries/recommended` - Рекомендации

### Аналитика
- `GET /api/analytics/summary` - Сводная статистика
- `GET /api/analytics/lottery/<id>` - Статистика по лотерее

## 🎨 Дизайн

Приложение использует минималистичный черно-желтый дизайн с:
- Свечением курсора мыши
- Плавными анимациями
- Адаптивной версткой
- Современной типографикой (Inter)

## 🧪 Тестирование

### Проверка подключения к БД
```bash
python -c "from database.db import Database; Database.initialize(); print('DB OK')"
```

### Проверка Ollama
```bash
python -c "from llm.ollama_client import OllamaClient; client = OllamaClient(); print('Ollama OK' if client.test_connection() else 'Ollama FAIL')"
```

### Проверка парсера
```bash
python parsers/stoloto_parser.py
```

## 📝 База данных

Схема включает следующие таблицы:
- `lotteries` - Информация о лотереях
- `draws` - Результаты тиражей
- `prize_categories` - Категории призов
- `users` - Пользователи
- `user_preferences` - Предпочтения пользователей
- `user_stats` - Статистика
- `recommendations` - История рекомендаций
- `chat_history` - История чата

## 🔐 Безопасность

- JWT токены для аутентификации
- Хеширование паролей (Werkzeug)
- Rate limiting на API endpoints
- CORS настройки
- Валидация входных данных

## 🚧 Возможные улучшения

- Реальный парсинг сайта Stoloto (сейчас используются fallback данные)
- Расширенная аналитика с графиками
- Push-уведомления о новых тиражах
- Экспорт статистики в PDF
- Социальная авторизация
- Мобильное приложение

## 📄 Лицензия

MIT

## 👥 Автор

Разработано для хакатона hackMSK52

---

**Важно**: Это демонстрационный проект. Для production использования:
1. Настройте реальный парсинг Stoloto
2. Добавьте более надежную аутентификацию
3. Настройте HTTPS
4. Используйте production WSGI сервер (gunicorn/uvicorn)
5. Добавьте мониторинг и логирование
6. Настройте резервное копирование БД
